class Sham {
    public static void reverseString(char[] s) { //создаем переменную s и кладем в неё массив char. char[] это тоже самое что и String.
        int len = s.length;                     //создаем переменную int len которая содержит длину массива s
        if (len == 0) return;                  //если длина массива равна 0, то и выводить ничего не надо. Вывод строки наоборот.

        for (int i = 0; i < (len / 2); i++) { //Проходимся по строке “abcd” в цикле. (Строка “abcd”).  Сначала i = 0, len = 4

            char l = s[i]; //в цикле записываем в l = s[0]. То есть нулевой элемент в строке. То есть l = “a”
            
/*затем на нулевое место записываем элемент на позиции len - i - 1. Len = 4, i = 0.
Итого получаем s[0] = s[4-1-0], то есть s[0] = s[3], то есть первой буквой строки становится последняя буква (s[3])
После этого действия строка равна не “abcd”, а “dbcd” */

            s[i] = s[len - i - 1]; 
            
/*тут на позицию 3 записываем то, что сохранили в отдельную переменную l
то есть s[3] = l, то есть s[3] = “a”
по итогу получаем строку “dbca”*/

            s[len - i - 1] = l; 
        }
        System.out.println(s);
    }
}

/*затем увеличивается i и становится равен 1
char l = s[i];
в цикле записываем в l = s[1]. То есть первый элемент в строке. То есть l = “b”

s[i] = s[len-i-1]; 
затем на первое место записываем элемент на позиции len - i - 1. Len = 4, i = 1. 
Итого получаем s[1] = s[4-1-1], то есть s[1] = s[2], то есть второй буквой строки становится третья буква (s[2])
После этого действия строка равна не “dbca”, а “dсca”

s[len-i-1] = l;
тут на позицию 2 записываем то, что сохранили в отдельную переменную l
то есть s[2] = l, то есть s[2] = “b”
по итогу получаем строку “dcba”
*/
